[project]
name = "einsum"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "huggingface-hub",
    "safetensors",
    "numpy",
    "numba>=0.64.0",
    "scipy>=1.15.3",
]

[project.optional-dependencies]
ray = ["ray"]
